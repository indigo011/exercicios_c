#include <stdio.h>

int main() {
    /*
    Arithmetic operators

    + (addition) for instance, 1 + 2 = 3
    - (subtraction) for instance, 3 - 2 = 1
    * (multiplication) for instance, 3 * 2 = 6
    / (division) for instance, 10 / 2 = 5
    % (modulus) for instance 3 % 2 = 1
    ++ (increment) for instance, 1++ = 2
    -- (decrement) for instance, 3-- = 2;
    
    */
}